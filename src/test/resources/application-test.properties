# Test Profile Configuration
# Auto-start the engine for integration tests (backward compatibility)
# Note: EngineControlIntegrationTest explicitly manages engine state
worker.simulation.auto-start=true
